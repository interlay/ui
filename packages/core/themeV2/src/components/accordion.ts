import { Spacing } from '../core';

type AccordionTheme = {
  padding: Spacing;
  minHeight: Spacing | string;
  region: {
    paddingX: Spacing;
    paddingBottom: Spacing;
  };
};

export type { AccordionTheme };
