import { DrawerTheme } from '../../components';

import { color } from './colors';

const drawer: DrawerTheme = {
  background: color('grey-300'),
  borderRight: `1px solid ${color('grey-200')}`
};

export { drawer };
